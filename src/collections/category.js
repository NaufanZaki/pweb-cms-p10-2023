/** @type {import('payload/types').CollectionConfig} */
const category = {
    slug: "category",
    fields: [
      {
        name: "name",
        type: "text",
        required: true,
      },
    ],
  };
  
  export default category;
  